/** Concentric nested settings card: recessed muted fill at --r-card-nested with label + hint header. */
export interface KnobProps {
  label: string;
  hint?: string;
  children?: any;
}
export declare const Knob: (props: KnobProps) => any;
