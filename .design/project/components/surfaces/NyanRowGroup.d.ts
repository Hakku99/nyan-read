/** Grouped card stack — children separated by inset hairlines inside one rounded shell. */
export interface NyanRowGroupProps {
  children?: any;
  style?: object;
}
export declare const NyanRowGroup: (props: NyanRowGroupProps) => any;
