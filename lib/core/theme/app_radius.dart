import 'nyan_radius.dart';

/// Semantic alias layer for consistent corner language in feature code.
class AppRadius {
  const AppRadius._();

  static const double small = NyanRadius.small;
  static const double medium = NyanRadius.input;
  static const double large = NyanRadius.card;
  static const double panel = NyanRadius.panel;
  static const double sheet = NyanRadius.sheet;
}
